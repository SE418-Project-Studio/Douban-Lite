spring:
  application:
    name: book-info-service

server:
  port: 8082

api:
  key: 15b7a9d7cdcc3bc3fe42316a4b551929

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true